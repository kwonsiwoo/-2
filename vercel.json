{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/|landing$|landing/|privacy$|privacy/).*)", "destination": "/index.html" }
  ]
}
